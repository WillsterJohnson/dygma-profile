# Dygma Profile

Keyboard layout I'm currently using on my Dygma Defy keyboard.

History can be accessed via git history.

To create a repository to track your own layout, clone the initial commit;

```sh
git clone git@github.com:WillsterJohnson/dygma-profile.git ./my-dygma-profile
git reset --hard 0bd4714
```
